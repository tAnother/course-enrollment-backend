apply plugin: "war"

bootWar {
    mainClass = "com.redpanda.courseregs.CourseEnrollmentBackendApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
